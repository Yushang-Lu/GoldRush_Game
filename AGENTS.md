# GoldRush 2.0 C++ 策略开发规范

本文件适用于整个仓库。用户在当前任务中的明确指令优先于本文件；若没有新的明确授权，必须遵守以下约束。

## 任务目标

- 使用 C++17 开发可提交的 GoldRush 2.0 策略共享库。
- 正确性是硬门槛：`moveDecision` 每轮都必须返回 ABI 与取值均合法的结果，不能崩溃、超时或产生未定义行为。
- 策略应兼顾实战质量与延迟，不能为了微基准成绩退化成明显无竞争力的策略。
- 延迟目标：在有代表性的 Release 基准中，`moveDecision` 的 P90 小于 200 微秒；尽量保留波动余量。只能把本地结果称为“本地实测”，最终以赛事服务器为准。

## 修改范围（硬约束）

- 除本文件是应用户本次要求创建外，后续所有新增、修改、移动和删除都只能发生在仓库根目录的 `./code/` 内。
- `./docs/`、`./game/`、仓库根目录中的其他文件以及 `.git/` 都是只读的。不得修改、格式化、移动、删除或向其中生成构建产物。
- 可以读取 `docs/` 和 `game/`，也可以从 `code/` 的构建脚本引用 `../game/game_api.h`。
- 构建产物、测试程序、基准结果和临时文件只能写入 `./code/` 或系统临时目录（如 `/tmp`）。不要在 `game/` 中直接执行会生成 `player.so` 的 `make`。
- 保留用户已有改动。不得顺手清理、重排或覆盖任务范围外的内容，也不要创建 Git 提交，除非用户明确要求。
- 如果完成任务确实需要修改 `./code/` 之外的文件，先停止并向用户说明原因、目标文件和所需改动，获得明确授权后再做。

## 权威资料与冲突处理

开始设计或修改策略前，先阅读当前版本的：

1. `game/game_api.h`：C++ ABI、常量和结构体布局的唯一权威来源。
2. `docs/faq.md`：规则细节和官方澄清。
3. `docs/rule.md`：完整赛事规则、计分和时限。
4. `game/README.md`、`game/player.cpp`、`game/Makefile`：官方参考实现与构建方式。

不要凭记忆替代仓库内文档。若资料有冲突：ABI 与布局以 `game/game_api.h` 为准，规则语义优先采用 FAQ 中更新、更具体的官方答复；仍无法消解时，明确记录冲突并询问用户，不得默默猜测或修改官方文件。

## `code/` 中的推荐结构

实现可以按复杂度拆分，但所有自有代码都必须位于 `code/`。推荐至少提供：

- `code/player.cpp`：策略入口与实现，或策略入口加若干 `code/*.h` / `code/*.cpp`。
- `code/Makefile`：默认构建 `code/player.so`。
- `code/README.md`：简述策略、构建/测试/基准命令，以及仍存在的假设。
- `code/tests/` 或等价文件：合法性、状态重置、规则边界和性能基准。

优先直接包含官方头文件，例如通过 `-I../game` 使用 `#include "game_api.h"`；除非有充分理由，不要复制并维护第二份 ABI 头文件。

期望的构建接口：

```bash
make -C code          # 生成 code/player.so
make -C code test     # 运行正确性测试（若已实现）
make -C code bench    # 运行 Release 性能基准（若已实现）
make -C code clean    # 只清理 code/ 内的产物
```

## ABI 与输出正确性

- 必须精确实现并导出：

  ```cpp
  extern "C" GameOutput moveDecision(const GameInput* input);
  ```

- 直接使用官方 `GameInput`、`GameOutput` 和常量；不要改变结构体、字段类型、对齐、打包方式或函数签名。
- 不修改 `input` 指向的数据，不保存会在调用后失效的输入指针或其内部地址。
- 每次调用都完整初始化输出。任何兜底路径都应显式返回 6 个“不动”动作（值 `4`），而不是依赖 `{}` 初始化——零值动作代表“上”，并非“不动”。
- 始终保证：每个 `actions[i]` 在 `[0,4]`，`k` 在 `[0,6]`，`order` 在 `{0,1}`，`vp` 在 `{0,1,2}`。
- 不允许异常越过 C ABI 边界。热路径应采用不会抛异常的实现；若调用了可能抛异常的代码，必须在函数内部捕获并返回合法兜底结果。
- 所有数组访问都要有边界依据。即使正常引擎会传合法数据，也应在读取计数型字段时做低成本的合理防御。
- 全局/静态状态会跨回合保留。必须在新对局（至少 `round == 0`）时可靠重置，并能防御轮数回退或非连续调用造成的旧状态污染。
- 不要把迷雾 `-5` 当作空地；不要假定 `visible_enemies[0/1]` 对应固定敌方角色；不要把当前视野误当成已探索地图。若保存历史地图，必须标记信息的新旧程度。
- 生成动作前，应按规则模拟两个己方角色按 `k` 与 `order` 顺序执行后的路径，处理边界、障碍和角色占位，避免策略自身制造可预见的冲突。被阻挡的一步不会取消后续动作。
- 当前规则参数和易错语义必须从官方资料确认，包括但不限于拾取比例、炸弹、NPC 踩踏、视野购买时序与成本、快照统计范围以及终局计分；不要从示例策略推导规则。

## 策略质量原则

- 先建立永远返回合法结果的可靠基线，再逐步提高搜索、估值和对手建模强度；每次优化都保留合法兜底路径。
- 对可见信息、历史信息和推断信息分别建模，不能把未公开的随机分布、NPC 策略或隐藏地图当成已知事实。
- 估值至少应考虑可获取金币、实际拾取比例、移动成本、炸弹/踩踏风险、障碍与角色碰撞、探索价值、对手竞争、区域快照信息和购买视野的终局净成本。
- 两个己方角色要联合规划，避免重复争夺同一目标或互相封路，并评估 `k` 和 `order`，而不只是独立选择方向。
- 使用有固定上界的候选生成与搜索。遇到时间压力或异常状态时提前退出并返回当前最佳合法方案。
- 如使用随机性，采用低开销、可复现且明确初始化的 PRNG；不要在 `moveDecision` 中调用 `std::random_device`。测试应能固定种子复现问题。
- 不得利用系统漏洞、访问未授权信息，或在策略中进行网络访问、进程操作及比赛无关的系统探测；遵守 `docs/faq.md` 中的诚信要求。

## 性能规范

- 热路径禁止日志、终端输出、文件/网络 I/O、休眠、子进程和阻塞同步。不要把调试输出留在提交版本中。
- 优先使用固定大小数组、紧凑结构、预分配缓冲区、整数运算和可复用状态。避免每回合堆分配、容器反复扩容、昂贵格式化、异常控制流及不受控搜索。
- 不要为了速度引入越界、溢出、未初始化读取、严格别名违规或其他未定义行为；这些问题可能只在评测机或优化构建中出现。
- Release 构建至少使用 `-std=c++17 -O2 -DNDEBUG -fPIC -shared`，并启用合理警告。可以在测量后选择 `-O3`、LTO 等优化。
- 本机与官方运行机 CPU 不同。默认提交构建不要无条件依赖本机的 `-march=native`；如需 native 优化，应做成显式选项，并在赛事提供、与运行机一致的编译环境中生成最终 `.so`。
- 尽量只依赖赛事环境明确提供的标准运行库。引入第三方动态库前必须核对评测机是否存在，不能以本机 `ldd` 成功作为唯一依据。
- `.so` 必须小于 16 MiB，并保留可被 `dlsym` 找到的动态符号 `moveDecision`。

## 必须执行的验证

修改策略后，按改动风险执行并报告以下验证；不得仅凭代码阅读宣称完成：

1. **构建检查**
   - Release 构建无错误，认真处理新增警告。
   - 用 `file code/player.so` 确认是 Linux x86-64 共享库。
   - 用 `nm -D --defined-only code/player.so` 确认精确导出 `moveDecision`。
   - 用 `ldd` / `readelf` 检查依赖和所需版本，并检查文件大小小于 16 MiB。

2. **正确性测试**
   - 覆盖全迷雾、地图边角、障碍、金币、炸弹、可见敌人、0 到最大数量 NPC、有效/无效快照等输入。
   - 覆盖连续 `0..499` 回合、新一局回到第 0 回合、轮数异常跳变和策略内部兜底路径。
   - 对大量随机但结构合法的 `GameInput` 做模糊测试，逐次验证输出所有字段范围。
   - 使用单独的 ASan/UBSan 调试构建检查越界和未定义行为；不要用 sanitizer 构建衡量延迟或作为提交产物。
   - 如果测试无法覆盖真实引擎行为，明确说明缺口，不得虚构通过结论。

3. **性能基准**
   - 使用与提交版本一致的 Release 优化，绕过任何调试日志和 sanitizer。
   - 先预热，再对有代表性的混合输入测量足够样本（建议至少 50,000 次），且保证编译器不能消除调用及结果使用。
   - 至少报告样本数、编译参数、机器、P50、P90、P99 和最大值；重复运行以识别调度抖动，并单独关注首次调用成本。
   - 验收目标是本地重复测试中 P90 < 200 微秒。若未达到，继续分析热点和优化；若当前环境无法可靠测量，要如实说明，不能估算成“已达标”。
   - 微基准只验证计算延迟。策略强度应结合用户提供的对局日志、回放或可用评测结果验证，不得用更快来替代胜率评估。

## 工作方式与交付

- 开始前查看 `code/` 现状和 `git status`，理解已有设计及用户改动，再进行最小范围修改。
- 对规则或性能有实质影响的改动，应说明依据、假设和权衡；不要声称知道官方未公开的信息。
- 诊断任务只诊断；用户要求实现或优化时，应完成代码、构建和与风险相称的验证，而不只给建议。
- 最终回复必须简洁列出：修改了哪些 `code/` 文件、策略变化、执行过的测试、实测延迟、产物位置，以及尚未验证的风险。
- 不得把本地基准描述成赛事成绩，也不得在没有测量时声称满足 P90 目标。
